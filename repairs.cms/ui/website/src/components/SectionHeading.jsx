import React from 'react'

const SectionHeading = ({children}) => {
  return (
    <div className='font-bold my-5 text-center text-[32px] '>
      {children}
    </div>
  )
}

export default SectionHeading
